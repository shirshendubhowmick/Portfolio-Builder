import React from 'react';
import classes from './MaxStockToast.css'

const maxStockToast = () => {
    return (
        <div id={classes.maxStockToast}>Max number of stocks added</div>
    );
}

export default maxStockToast;